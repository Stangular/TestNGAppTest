import { BarLayer } from '../../../../canvas/models/custom/layers/charts/content/bars/bar.layer';

//export class SSSBar extends BarLayer{

//  constructor(dataService: AppDataService, margins: Margin, size: Size,
//    private data: { xparam: number, yparam: number }[] = []) {
      
//  }

//  XScale(): IScale {
//    let xScale = new NumericScale(0, 100, 20, 5);
//    return new NumericScale(0, 100, 20, 5);
//  }

//  YScale(): IScale {
//    return new TimeScale(d1, d2, 1, TimeRangeType.day, ['S', 'M', 'T', 'W', 'T', 'F', 'S']);
//  }
//}

import { Injectable } from '@angular/core';
//import { ITreeNode } from 'src/models/Interface/IIndexer';

@Injectable({
  providedIn: 'root'
})
export class TreeviewService {
//  private _selectedNode: any = null;
 // private _nodePath: any [];
  constructor() {
   // this._selectedNode = this._nodes[0];
  //  this._nodePath = [];
  }

  //SelectNode(id: string) {
  //  let node = this._selectedNode.children.find(n => n.id == id);
  //  if (node) {
  //    this._nodePath.push(this._selectedNode);
  //    this._selectedNode = node; 
  //  }
  //}
  //get ChildNodes() {
  //  if (this._selectedNode) {
  //    return this._selectedNode;
  //  }
  //  return [];

  //}

  //MoveUp() {
  //  if (this.PathCount <= 0) { return null; }
  //  this._selectedNode = this._nodePath.pop();
  //}
  //get SelectedNode() { return this._selectedNode; }
  //get PathCount() { return this._nodePath.length; }
}
